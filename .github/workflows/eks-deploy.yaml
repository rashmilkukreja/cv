name: Deploy to EKS

on:
  workflow_dispatch:

jobs:
  note:
    name: Use CI/CD Deploy to EKS
    runs-on: ubuntu-latest
    steps:
      - name: Deployment workflow moved
        run: echo "Use .github/workflows/ci-cd.yaml for the full build, test, and EKS deployment pipeline."
